#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        int n, k;
        cin >> n >> k;

        int p = n * k;
        long long sum = 0;
        while (k--)
        {
            pointer -= (n / 2 + 1);
            sum += v[pointer];
        }
        cout << sum << endl;
    }
}